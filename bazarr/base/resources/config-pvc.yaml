apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bazarr-config
  annotations:
    folder: media-server/bazarr/config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: nfs-ssd-1
