apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jellyseerr-config
  annotations:
    folder: media-server/jellyseerr/config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: nfs-ssd-1
