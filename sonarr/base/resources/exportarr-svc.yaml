apiVersion: v1
kind: Service
metadata:
  name: sonarr-exportarr
  labels:
    app: sonarr
spec:
  clusterIP: None
  ports:
    - name: http-metrics
      port: 9707
      targetPort: http-metrics
  selector:
    app: sonarr
