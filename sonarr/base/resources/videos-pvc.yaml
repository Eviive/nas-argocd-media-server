apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarr-videos
  annotations:
    folder: videos
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 400Gi
  storageClassName: sonarr-nfs
