apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qbittorrent-gluetun-config
  annotations:
    folder: media-server/qbittorrent-gluetun/config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: nfs-ssd-1
